<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ranger.xyg.xygapp">

    <application
        android:name=".XygApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".demos.JSBridgeTestActivity"/>

        <activity android:name=".demos.RetrofitDemoActivity"/>

        <activity android:name=".demos.rxjava.RxSimpleActivity"
            android:theme="@style/AppTheme.Transparent"/>

        <activity android:name=".demos.rxjava.MoreRxDemosActivity"
            android:theme="@style/AppTheme.Transparent"/>

        <activity android:name=".demos.recycleview.RecyclerViewDemoActivity"
            android:theme="@style/AppTheme.Transparent"/>

        <activity android:name=".demos.SampleHomeActivity"
            android:theme="@style/AppTheme.Transparent"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>